node_modules/
dist/
studio-dist/
coverage/
.DS_Store
*.log

# Credentials and key material. This repo is public, so a stray .env or key must
# never become committable; only *.example placeholders are tracked.
.env
.env.*
!.env.example
!.env.sample
*.pem
*.key
